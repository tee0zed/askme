<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8"/>
  <title>Askify</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css">
  <%= csrf_meta_tags %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>

<div class="header">
  <div class="header-logo">
    <h1 class="text-medium text-center"><%=link_to "Askify", root_path %></h1>
  </div>
  <div class="header-nav">
    <div class="header-nav">
      <% if   current_user %>
        <%= link_to "@#{current_user.username}", user_path(current_user) %> &nbsp
        <%= link_to 'Выйти', log_out_path,
                    class: 'button small' %>
      <% else %>
        <%= link_to 'Зарегистрироваться', sign_up_path,
                    class: 'button small' %>
        <%= link_to 'Войти', log_in_path,
                    class: 'button small' %>
      <% end %>
    </div>
  </div>
</div>

<div class="container color-<%=container_color%>">
  <% if flash.any? %>
    <div class="margin-both question-box">
      <% flash.each do |name, message| %>
        <%= content_tag :div, message, class: "flash-alert" %>
      <% end %>
    </div>
  <% end %>
    <%= yield %>
</div>

</body>
</html>
